---
name: deep-cuts-factory
description: Research, configure, validate, auto-publish and deliver one permanent Deep Cuts artist-discovery edition from a single "Deep Cuts [Artist]" instruction. Use for every new band or artist edition and for verified destination updates.
---

# Deep Cuts Factory

Read `PLATFORM_ARCHITECTURE_DIRECTIVE.md` first, then read `DEEP_CUTS_PRODUCTION_MANUAL.md`, `CLAUDE.md` and `ROADMAP.md` completely before acting. Complete the directive's impact assessment and preserve every unrelated edition.

Every generated or updated public screen must retain the Core Engine's immutable final footer: `Deep Cuts`, followed by `Copyright Clearlight Creative`. Never let edition configuration, product branding, quiz or result rendering replace or suppress it.

For `Deep Cuts [Artist]`:

1. Run `node scripts/start-edition.mjs "Artist"` before research. Never restart the clock.
2. Research current official artist-controlled sources first. Open and identity-check every proposed destination. When an official YouTube channel exists, verify its most-viewed official music video for the featured player. Record evidence using `references/research-contract.md`. Omit every unavailable destination.
3. Save the completed research input outside the edition folder, then run `node scripts/create-edition.mjs <input.json>`. This creates only edition configuration, evidence and the opaque route.
4. Run the complete validation and the edition QR scan-back. Fix failures without involving the owner.

The optimized Core Engine keeps these gates intact: `npm run validate` executes the complete plan through the bounded fail-closed runner, while the main deployment is the single automatic producer of the complete delivery-artwork package. Delivery artwork may be restored only from the exact-input cache and must still pass manifest SHA-256, dimensions and full-size plus 540-pixel QR scan-back before use. Use `npm run build:artwork` for an explicit complete local or manual rebuild; never restore the former repeated per-edition validation loop.

For `Deep Cuts Cars [Make and Model]`, follow the same clock, validation, opaque-route, QR, deployment and delivery workflow with `editionType: car`. Verify the automotive identity and the model-specific history, specifications, buyer guidance, authoritative video, owners' community, restoration parts, live sales and editorial sources. Do not alter Music definitions.

For `Deep Cuts Clubs [Club name and location]`, use `editionType: club` and the same unattended clock-to-delivery workflow. Verify the exact club and its direct official website, calendar, news, events, membership, public participation, competition, venue, history, contact, social and governing-body destinations. Omit login, share, search, generic and uncertain links. Do not alter Music or Cars definitions.

For an authorised Business Recruitment edition, use `editionType: business`. Verify the official company identity, logo, palette, website, careers, benefits, contact and every direct current vacancy page. Record any supplied video as `owner-selected` without implying official channel ownership. Include exactly ten positive sourced company questions. Use only the owner-approved edition artwork, protect its SHA-256 identity in an edition-specific test, and never replace the standard Aggits masters or change another product.

For `JookBox [Band]`, use `editionType: jukebox` and model `jookbox/3`. The final owner-approved product is **Mahogany Jukebox Master**, with Savage Garden `dc_e65763b78b` as its live reference after the final mobile coin-motion and four-key proportion corrections. Capture the owner-supplied artist-controlled source as a dated research snapshot, independently verify every destination that may be published, preserve its source title in evidence, and feature the most-viewed embeddable official video visible through the verified channel at research time. Never publish runtime-scraped or uncertain links. Never configure Aggits, a quiz or a wheel in the live Band page. Every new, migrated or regenerated edition must use the permanent `mahogany-jookbox-master/1` appearance and `mahogany-four-key/1`; reject ATLAS, neon, an alternative cabinet asset or another key format. This is the SHA-256-locked mahogany/brass cabinet, live marquee, hero ticker, real coin bay, YouTube screen, exactly four circular icon-only keys, working brass `SHARE` plate and lower `Copyright Clearlight Creative 2026.` hardware. Fit every band name after fonts load and on resize; preserve the approved scale when it fits, then reduce and balance onto multiple lines only when necessary, never truncating it. Use the configuration-driven four-state wake-up: `sleeping`, `acceptingCoin`, `poweringUp`, `awake`. The accessible coin visibly inserts while it plays the sourced, licensed local real coin-slot recording after direct interaction; protect that sound's mobile media focus before requesting YouTube autoplay and retain the manual play control for browser-policy fallback. Keys remain dimmed, unfocusable and unavailable before the coin, unlock together at `awake`, and never sequence, glow or pulse. The sourced uppercase biography ticker moves right-to-left with large bright type above the video. Store only the edition-specific awake flag in `sessionStorage`. Publish only four explicit verified IDs in `displaySelectionIds`, never a runtime source interpretation. Generate the owner-approved `aggits-character-poster-perspective/2` delivery QR; Aggits appears only on that downloadable/email poster, never on the live Band page. Perspective-fit its permanent opaque URL and require actual full-size, 627-pixel and 360-pixel decoder scan-back. Completed alternative routes stay frozen solely for compatibility and are never templates. The ten-edition trial is complete; all future Band work uses this master.

Apply the shared origin-aware link helper to every published JookBox anchor. External destinations use `target="_blank"` and `rel="noopener noreferrer"` so the cabinet stays open in its original tab; same-origin and relative navigation remains unchanged.

For every new mahogany Band JookBox, supply exactly four verified external `displaySelectionIds` in display order. The renderer must produce exactly four uniform circular icon-only keys, retain their labels only for accessibility and analytics, and render no key lighting. The large brass `SHARE` plate is the sole working Share action, and the lower status hardware reads `Copyright Clearlight Creative 2026.` while retaining the immutable global footer.

For Mahogany Jukebox Windows candidate intake, `ADD 20 BANDS` is draft-only and requires no operator filters. Shortlist musical groups through one structured Wikidata query requiring Linktree, Bandcamp, Instagram, Facebook and Spotify identifiers, use general search only as fallback, then resolve every destination through the direct 98% identity gate and reject incomplete results. Grade verified embeddable official YouTube as gold; otherwise grade the complete four-platform set silver and leave media for owner completion. Never publish from intake. Archive deletion is allowed only before any permanent edition identity exists.

For `Laneway [Band or Artist]`, use `editionType: laneway`. Preserve the exact approved Laneway Music source logo and display its deterministic transparent reverse-white rendition on the charcoal/black Laneway interface; never depend on CSS blending that may expose a rectangular background. Never configure or generate Aggits. Verify the official Laneway Music artist page and use its selected YouTube feature immediately below `Listen / Watch / Discover / Buy`. Put the five-question challenge after verified artist destinations, immediately above the fixed `Home / Share / Recommended` row. Home and Recommended use the official Laneway Music home/artist catalogue. Research exactly five positive, informative band questions, each with four unique choices, one best answer, a useful explanation and matching dated identity-verified HTTPS evidence. Save them to the isolated `laneway-questions.json`; do not alter any other edition's question or display rules.

The standalone `laneway_company` wheel is not the `Laneway [Band or Artist]` factory output and is not the `indie_wheel` model. Preserve its unified pale-teal decorative system with red used only by the illuminated wheel pointer, light-blue winner-purchase heartbeat, compact accessible centre-button spinning spiral, conditional verified winner-video panel, one-time visibility-aware pale-blue quiz invitation 10 seconds after the first completed result, 10-question Laneway artist quiz and accepted invitation-reveal analytics event only on `dc_b9e7b66620`. Winner videos belong in the isolated artist-video map, load through `youtube-nocookie.com` only after selection, and are omitted when a playable identity-matched video cannot be verified. Never propagate those presentation rules to artist-specific Laneway or Cool Death editions.

`indie_label/1` is the final locked commercial reference for future independent-label wheels. Its approved reference state is the restored `dc_b9e7b66620` experience described in `INDIE_LABEL_MODEL.md`; this does not change its `laneway_company` edition type or make another edition depend on Laneway configuration. Create future labels only through isolated `indie_wheel` configuration, changing the new edition's verified brand, palette, roster, destination platform, links, copy, quiz, contact and source-credit metadata. Never replace the global platform footer, reintroduce the rejected catalogue-discovery overhaul or modify Laneway/Cool Death to produce a new label.

For a Record Company Edition, trigger `.github/workflows/record-company-build.yml` with the one official company URL. The workflow owns the full eligible roster without further approval. Monitor it through completion; artists below the 98% identity/question gate are exceptions, not blockers. Do not add Aggits. Report success only after the master QR grid, reporting package, generated-assets pull request and completion email are confirmed.
5. Create an `agent/deep-cuts-<slug>` branch and pull request through the connected GitHub app. Mark it ready and wait for green validation. Enable auto-merge when the repository allows it; otherwise merge through the authorised GitHub connector only after the head SHA and green checks are re-confirmed. Do not ask the owner to merge or use PowerShell.
6. Monitor the main-branch deployment. It regenerates and verifies artwork, registers analytics, emails the live opaque URL and QR PNG, and waits for the signed `email.delivered` webhook.
7. Report the live link only after the deployment and confirmed email delivery are green. Report exceptions only when credentials, ambiguous identity or missing authority make safe completion impossible.

For a destination update, modify only the verified edition configuration and evidence, preserve the existing `editionId`, validate, auto-merge and deploy. The QR must not change.

For an explicit owner-selected Laneway video replacement, verify the supplied YouTube identity and privacy-enhanced embed, record `selectionBasis: owner-selected` with `ownerSelected: true`, update matching research evidence and preserve the official-label default for every future edition.

Never create another repository, expose a band name in a public URL, invent a link or video, redraw Aggits, bypass QR scan-back, or ask the owner to perform repeatable engineering work. Analytics is best effort and must never interrupt navigation. Every repeated manual step is an automation opportunity.

Deep Cuts Studio is an optional local intake, verified-research and preview layer, not a publication authority. For JookBox projects, only a passed `deep-cuts-studio-jookbox-research/1` record at the mandatory 98% gate may populate preview destinations; raw names, URLs, YouTube leads, briefs and MP3s remain research leads. A `deep-cuts-studio-project/1` draft or `deep-cuts-studio-handoff/1` export must still follow the isolation, configuration, QR, deployment and live-verification stages above. Studio remains local-only inside its sandboxed desktop application and must never expose a public administrative Worker route.

