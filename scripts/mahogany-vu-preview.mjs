import { aggitsJukeboxIcon } from "./aggits-jukebox-icons.mjs";

export const MAHOGANY_VU_RENDERER_VERSION = "mahogany-vu-jukebox/2026-08-10-v4";
export const MAHOGANY_VU_TITLE_REVEAL_SECONDS = 3;
export const MAHOGANY_VU_TITLE_BLINK_SECONDS = 3;
export const MAHOGANY_VU_TITLE_BLINK_HALF_CYCLE_SECONDS = 0.375;

export function renderMahoganyVuPreview(
  project,
  { musicUrl = "", characterUrl = "", canonicalUrl = "" } = {},
) {
  const input = project.input || {},
    actions = (input.actionButtons || []).slice(0, 4),
    musicReady = Boolean(project.vu?.music?.fileName && musicUrl),
    characterReady = Boolean(project.vu?.character?.fileName && characterUrl),
    ducking = project.vu?.ducking || {},
    duckingEnabled = ducking.enabled !== false,
    speakingLevel = Math.max(0.08, Math.min(0.5, Number(ducking.speakingLevel) || 0.2)),
    attackMs = Math.max(150, Math.min(700, Number(ducking.attackMs) || 320)),
    releaseMs = Math.max(300, Math.min(1800, Number(ducking.releaseMs) || 800)),
    speechRegions =
      ducking.analysis?.status === "complete" &&
      ducking.analysis?.sourceSha256 === project.vu?.character?.sha256
        ? ducking.analysis.regions || []
        : [],
    jukeboxTitle = String(input.name || "Mahogany VU Jukebox").trim(),
    jukeboxTitleClass =
      jukeboxTitle.length > 34
        ? " is-very-long"
        : jukeboxTitle.length > 22
          ? " is-long"
          : "",
    keys = actions
      .map((action) => {
        const icon = aggitsJukeboxIcon(action.iconId),
          href = safeDestination(action.href);
        return `<a class="action" href="${esc(href || "#")}" data-enabled="${href ? "true" : "false"}" aria-label="${esc(action.label || `Key ${action.slot}`)}"><span class="action-icon"><img src="${esc(icon?.assetPath || "")}" alt=""></span></a>`;
      })
      .join(""),
    character = characterReady
      ? `<video id="characterVideo" src="${esc(characterUrl)}" playsinline preload="metadata" crossorigin="anonymous" hidden></video><canvas id="characterCanvas" class="character" aria-label="Optional Aggits character video layer"></canvas>`
      : "";
  return `<!doctype html><html lang="en-AU"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#090402"><title>${esc(input.name || "Mahogany VU Jukebox")}</title><style>
  *{box-sizing:border-box}html,body{margin:0;min-height:100%;background:#020100;color:#efd49c;font-family:Arial,sans-serif}body{display:grid;justify-items:center}.stage{width:min(100vw,768px);padding-bottom:20px}.machine{position:relative;width:100%;aspect-ratio:768/1280;overflow:hidden;background:#050201 url('/assets/aggits-jukebox-vu-master-v1.jpg') center/100% 100% no-repeat;filter:brightness(.55) saturate(.75);transition:filter .75s}.machine.is-awake{filter:brightness(1) saturate(1)}
  .ticker{position:absolute;z-index:4;top:19.45%;left:12.9%;width:74.6%;height:5.6%;overflow:hidden;display:flex;align-items:center;background:#070402;color:#ffd78a;font:700 clamp(8px,2.35vw,17px)/1 'Courier New',monospace;letter-spacing:.08em;text-transform:uppercase;opacity:.22;transition:opacity .5s}.is-awake .ticker{opacity:1}.ticker span{display:inline-block;min-width:100%;padding-left:100%;white-space:nowrap;animation:ticker 18s linear infinite}@keyframes ticker{to{transform:translateX(-100%)}}
  .equipment{position:absolute;z-index:3;top:25.84%;left:23.55%;width:64.78%;height:29.3%;overflow:hidden;border:1px solid #4f2e17;background:radial-gradient(circle at 50% 10%,#6c3a1e 0,#27150d 42%,#090503 100%);box-shadow:inset 0 0 0 3px #170b06,inset 0 0 22px #000}.meters{position:absolute;left:4%;top:5%;width:92%;height:39%;display:flex;gap:3%}.meter{position:relative;flex:1;overflow:hidden;border:clamp(2px,.55vw,4px) solid #462714;border-radius:5% 5% 10% 10%;background:radial-gradient(ellipse at 50% 105%,#f8dc8c 0,#d4a75b 54%,#7a5225 100%);box-shadow:inset 0 0 10px #4c2b14,0 2px 5px #000}.scale{position:absolute;inset:9% 8% auto;text-align:center;color:#3a1b0a;font:700 clamp(5px,1.3vw,10px)/1 Georgia,serif;letter-spacing:.12em}.scale:before{content:'20  10  7  5  3  2  1  0  1  2  3';display:block;font:clamp(3px,.75vw,6px)/1 Arial,sans-serif;letter-spacing:0}.scale strong{display:block;margin-top:12%;font-size:1.45em}.needle{position:absolute;z-index:2;left:50%;bottom:-5%;width:43%;height:1px;background:#2a1008;box-shadow:0 0 1px #000;transform-origin:0 50%;transform:rotate(-68deg);transition:transform .06s linear}.meter:after{position:absolute;left:47%;bottom:0;width:6%;aspect-ratio:1;border-radius:50%;content:'';background:#241006;box-shadow:0 0 0 1px #c08b3e}
  .deck{position:absolute;left:3%;right:3%;top:46%;bottom:3%;border:1px solid #68401f;background:linear-gradient(145deg,#5b321a,#160b06 55%,#090403);box-shadow:inset 0 0 0 2px #1a0c06,inset 0 0 14px #000}.reel{position:absolute;top:8%;width:32%;aspect-ratio:1;border:clamp(2px,.55vw,4px) solid #8a663b;border-radius:50%;background:conic-gradient(from 0deg,#9d7a4b 0 8deg,transparent 8deg 55deg,#9d7a4b 55deg 63deg,transparent 63deg 110deg,#9d7a4b 110deg 118deg,transparent 118deg 165deg,#9d7a4b 165deg 173deg,transparent 173deg 220deg,#9d7a4b 220deg 228deg,transparent 228deg 275deg,#9d7a4b 275deg 283deg,transparent 283deg 330deg,#9d7a4b 330deg 338deg,transparent 338deg);box-shadow:inset 0 0 0 3px #201007,0 2px 5px #000}.reel:after{position:absolute;inset:38%;border-radius:50%;content:'';background:#120804;box-shadow:0 0 0 2px #9f7744}.reel.left{left:10%}.reel.right{right:10%}.is-playing .reel{animation:spin 1.75s linear infinite}.reel.right{animation-direction:reverse}@keyframes spin{to{transform:rotate(360deg)}}.tape{position:absolute;left:20%;right:20%;top:70%;height:5%;border-bottom:2px solid #080301;border-radius:50%}.transport{position:absolute;left:25%;right:25%;bottom:7%;height:12%;display:flex;justify-content:space-around}.transport i{width:13%;border:1px solid #8e6638;background:linear-gradient(#84552b,#251309);box-shadow:inset 0 1px #c89253}.character{position:absolute;z-index:5;right:1%;bottom:1%;width:31%;height:61%;object-fit:contain;object-position:right bottom;filter:drop-shadow(0 2px 2px #000)}
  .coin{position:absolute;z-index:8;top:31.9%;left:12.25%;width:10.5%;height:13.5%;padding:0;border:0;background:transparent;cursor:pointer;perspective:160px}.coin-art{display:block;width:100%;aspect-ratio:1;object-fit:cover;clip-path:circle(47% at 50% 50%);filter:drop-shadow(0 2px 4px #000) brightness(.94);transform-origin:8% 50%;animation:coinCall 2.8s ease-in-out infinite}.coin.is-accepting .coin-art{animation:coinInsert .62s cubic-bezier(.34,.02,.75,.34)}.coin:focus-visible{outline:2px solid #ffd471;outline-offset:2px}.coin-status{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap;border:0}.jukebox-title{position:absolute;z-index:8;top:39.55%;left:29%;width:42%;height:2.15%;display:flex;align-items:center;justify-content:center;overflow:hidden;color:#a9ff88;text-align:center;font:900 clamp(7px,1.52vw,12px)/1 "Arial Black","Rockwell Extra Bold",Impact,sans-serif;letter-spacing:.13em;text-transform:uppercase;white-space:nowrap;opacity:0;-webkit-text-stroke:.28px #294a21;background:radial-gradient(circle,#f2ffd7 0 18%,#9dff78 22% 43%,#4a8b35 47% 58%,transparent 62%) center/5px 5px repeat;-webkit-background-clip:text;background-clip:text;filter:drop-shadow(0 0 1px #77ff57);transition:opacity .08s linear}.jukebox-title.is-long{font-size:clamp(6px,1.22vw,10px);letter-spacing:.09em}.jukebox-title.is-very-long{font-size:clamp(5px,1.02vw,8px);letter-spacing:.055em}.jukebox-title.is-visible{opacity:.14}.jukebox-title.is-lit{opacity:1;filter:drop-shadow(0 0 1px #d9ffc4) drop-shadow(0 0 3px #68ef4b) drop-shadow(0 0 7px #2a7b20)}.actions{position:absolute;z-index:7;top:55.9%;left:12.05%;width:73.5%;height:18.2%;display:grid;grid-template-columns:repeat(4,1fr);gap:1.45%;background:transparent}.action{display:grid;place-items:center;border:0;border-radius:50%;background:transparent;opacity:.4;pointer-events:none;transition:filter .15s,transform .15s,opacity .4s}.is-awake .action[data-enabled="true"]{opacity:1;pointer-events:auto}.action:active{transform:translateY(2px) scale(.98);filter:brightness(1.28)}.action-icon{display:grid;place-items:center;width:68%;aspect-ratio:1}.action-icon img{display:block;width:100%;height:100%;object-fit:contain}.share{position:absolute;z-index:7;top:76.9%;left:12.7%;width:74%;height:6%;border:0;background:transparent;cursor:pointer}.footer{padding:9px 12px;text-align:center;color:#8c6c49;font-size:11px}.audio-note{margin:0 auto;padding:8px 12px;max-width:620px;color:#af8c5e;text-align:center;font-size:11px}.machine:not(.has-media) .coin{cursor:not-allowed}
  .ticker{top:18.8%;left:8.7%;width:80.1%;height:5.3%}.equipment{top:25.34%;left:18.76%;width:70.05%;height:33.3%;overflow:visible;border:0;background:transparent;box-shadow:none}.meters{inset:0;width:100%;height:100%;gap:0}.meter{position:absolute;overflow:visible;border:0;border-radius:0;background:transparent;box-shadow:none}.meter:first-child{top:12.8%;left:13.1%;width:35.4%;height:30%}.meter:nth-child(2){top:12.8%;left:51.3%;width:35.7%;height:30%}.scale,.meter:after{display:none}.needle{z-index:6;left:50%;bottom:7%;width:42%;height:clamp(1px,.2vw,2px);background:#2a1008;box-shadow:0 0 1px #000;transform-origin:0 50%;transform:rotate(-142deg)}.deck{inset:0;border:0;background:transparent;box-shadow:none}.reel{top:46.5%;width:22.2%;border:0;border-radius:50%;background:none;box-shadow:none;overflow:visible}.reel.left{left:19.4%;transform:rotate(-1.5deg) scaleY(.61)}.reel.right{right:20.1%;transform:rotate(1.5deg) scaleY(.61)}.is-playing .reel{animation:none}.reel-rotor{position:absolute;inset:7%;border-radius:50%;background:repeating-conic-gradient(from 4deg,rgba(255,218,150,.54) 0 3deg,transparent 3deg 58deg,rgba(70,34,12,.22) 58deg 60deg);mix-blend-mode:screen;opacity:.42;filter:blur(.15px);transform-origin:50% 50%}.reel-rotor:after{position:absolute;inset:42%;content:'';border-radius:50%;background:radial-gradient(circle,#f0c980 0 14%,#55341d 20% 52%,#ca9250 57% 67%,transparent 72%);box-shadow:0 0 2px #000}.is-playing .reel-rotor{animation:spin 1.75s linear infinite}.reel.right .reel-rotor{animation-direction:reverse}.reel:after,.tape,.transport{display:none}.character{right:1.5%;bottom:5%;width:29%;height:57%}.actions{top:59.2%;left:8.5%;width:80%;height:22.1%;gap:1.8%}.action-icon{opacity:0}.share{top:82.2%;left:9%;width:78%;height:5.7%}.coin{top:31%;left:7.9%;width:11.2%;height:auto;aspect-ratio:1}.copyright-plaque{position:absolute;z-index:7;left:25%;bottom:.55%;width:50%;height:1.65%;display:grid;place-items:center;border:1px solid #7b5028;border-radius:999px;color:#251207;background:linear-gradient(#d09a51,#7b481f);box-shadow:inset 0 1px #efc27d,0 1px 3px #000;font:700 clamp(4px,.82vw,7px)/1 Georgia,serif;letter-spacing:.02em}
  @keyframes coinCall{0%,63%,100%{filter:drop-shadow(0 2px 4px #000) brightness(.94)}78%{filter:drop-shadow(0 2px 5px #000) brightness(1.09)}}@keyframes coinInsert{0%{transform:translateX(0) rotateY(0);opacity:1}68%{transform:translateX(-38%) rotateY(68deg) scale(.9);opacity:1}100%{transform:translateX(-54%) rotateY(88deg) scale(.82);opacity:.08}}
  @media(prefers-reduced-motion:reduce){.ticker span,.is-playing .reel-rotor,.coin-art{animation:none}.machine{transition:none}}
  </style></head><body><main class="stage"><section id="machine" class="machine${musicReady || characterReady ? " has-media" : ""}" data-renderer-version="${MAHOGANY_VU_RENDERER_VERSION}" aria-label="${esc(input.name || "Edition")} Mahogany VU Jukebox preview"><div class="ticker"><span>${esc(input.tickerText || input.name || "MAHOGANY VU JUKEBOX")}</span></div><div class="equipment"><div class="meters"><div class="meter"><div class="scale">VU<strong>L</strong></div><i id="leftNeedle" class="needle"></i></div><div class="meter"><div class="scale">VU<strong>R</strong></div><i id="rightNeedle" class="needle"></i></div></div><div class="deck"><i class="reel left"><i class="reel-rotor"></i></i><i class="reel right"><i class="reel-rotor"></i></i><i class="tape"></i><div class="transport"><i></i><i></i><i></i><i></i><i></i></div></div>${character}</div><button id="coin" class="coin" type="button" aria-label="Insert the Aggits coin to play or pause the VU Jukebox"><img class="coin-art" src="/assets/aggits-coin-gold-v1.png" alt=""></button><div id="coinStatus" class="coin-status" aria-live="polite"></div><div id="jukeboxTitle" class="jukebox-title${jukeboxTitleClass}" aria-hidden="true">${esc(jukeboxTitle)}</div><div class="actions">${keys}</div><button id="share" class="share" type="button" aria-label="Share this Jukebox"></button><div class="copyright-plaque">Copyright Clearlight Creative 2026</div>${musicReady ? `<audio id="music" src="${esc(musicUrl)}" preload="metadata" crossorigin="anonymous"></audio>` : ""}</section><p class="audio-note">Twin VU needles respond to the uploaded music. Reels rotate in the tape deck's perspective only during playback.${characterReady ? " The optional keyed Aggits layer is ready." : " No character is included; the optional Aggits video layer is empty."}</p></main><script>
  (()=>{const machine=document.getElementById('machine'),coin=document.getElementById('coin'),music=document.getElementById('music'),character=document.getElementById('characterVideo'),canvas=document.getElementById('characterCanvas'),leftNeedle=document.getElementById('leftNeedle'),rightNeedle=document.getElementById('rightNeedle'),status=document.getElementById('coinStatus'),titleLight=document.getElementById('jukeboxTitle'),titleRevealSeconds=${MAHOGANY_VU_TITLE_REVEAL_SECONDS},titleBlinkSeconds=${MAHOGANY_VU_TITLE_BLINK_SECONDS},titleBlinkHalfCycleSeconds=${MAHOGANY_VU_TITLE_BLINK_HALF_CYCLE_SECONDS},duckingEnabled=${JSON.stringify(duckingEnabled)},speakingLevel=${JSON.stringify(speakingLevel)},attackMs=${JSON.stringify(attackMs)},releaseMs=${JSON.stringify(releaseMs)},speechRegions=${JSON.stringify(speechRegions)},shareUrl=${JSON.stringify(canonicalUrl || "")},shareTitle=${JSON.stringify(input.name || "Mahogany VU Jukebox")};let context,musicSource,musicGain,leftAnalyser,rightAnalyser,frames,raf=0,canvasContext,ducked=false,started=false;
  const rms=(analyser,data)=>{if(!analyser)return 0;analyser.getByteTimeDomainData(data);let sum=0;for(let i=0;i<data.length;i++){const n=(data[i]-128)/128;sum+=n*n}return Math.sqrt(sum/data.length)};
  const prepare=async()=>{if(context){await context.resume();return}if(music){const AudioContext=window.AudioContext||window.webkitAudioContext;if(!AudioContext)throw new Error('Web Audio is unavailable.');context=new AudioContext();musicSource=context.createMediaElementSource(music);musicGain=context.createGain();const splitter=context.createChannelSplitter(2);leftAnalyser=context.createAnalyser();rightAnalyser=context.createAnalyser();leftAnalyser.fftSize=512;rightAnalyser.fftSize=512;musicSource.connect(splitter);splitter.connect(leftAnalyser,0);splitter.connect(rightAnalyser,1);musicSource.connect(musicGain);musicGain.connect(context.destination);frames=[new Uint8Array(leftAnalyser.fftSize),new Uint8Array(rightAnalyser.fftSize)];await context.resume()}if(character&&canvas&&!canvasContext)canvasContext=canvas.getContext('2d',{willReadFrequently:true})};
  const drawCharacter=()=>{if(!character||!canvasContext||character.readyState<2)return;const width=180,height=Math.max(1,Math.round(width*(character.videoHeight||360)/(character.videoWidth||640)));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height}canvasContext.drawImage(character,0,0,width,height);const image=canvasContext.getImageData(0,0,width,height),data=image.data;for(let i=0;i<data.length;i+=4){const r=data[i],g=data[i+1],b=data[i+2],green=g-Math.max(r,b);if(g>70&&green>18){data[i+3]=Math.max(0,255-Math.min(255,(green-18)*7))}}canvasContext.putImageData(image,0,0)};
  const speechNow=()=>duckingEnabled&&character&&!character.paused&&!character.ended&&speechRegions.some(region=>character.currentTime>=region[0]&&character.currentTime<=region[1]);
  const setDuck=(next,immediate=false)=>{if(!musicGain||(next===ducked&&!immediate))return;ducked=next;const now=context.currentTime,target=next?speakingLevel:1,duration=(next?attackMs:releaseMs)/1000;musicGain.gain.cancelScheduledValues(now);musicGain.gain.setValueAtTime(musicGain.gain.value,now);musicGain.gain.linearRampToValueAtTime(target,now+(immediate?.01:duration))};
  const updateTitleLight=()=>{const time=music?.currentTime||0,revealEnd=titleRevealSeconds+titleBlinkSeconds,visible=Boolean(music&&time>=titleRevealSeconds),blinking=visible&&time<revealEnd,lit=visible&&(!blinking||Math.floor((time-titleRevealSeconds)/titleBlinkHalfCycleSeconds)%2===0);titleLight?.classList.toggle('is-visible',visible);titleLight?.classList.toggle('is-lit',lit)};
  const animate=()=>{const musicPlaying=Boolean(music&&!music.paused&&!music.ended),characterPlaying=Boolean(character&&!character.paused&&!character.ended);updateTitleLight();if(!musicPlaying&&!characterPlaying){raf=0;machine.classList.remove('is-playing');setDuck(false);return}machine.classList.toggle('is-playing',musicPlaying);const l=musicPlaying?Math.min(1,rms(leftAnalyser,frames[0])*4.4):0,r=musicPlaying?Math.min(1,rms(rightAnalyser,frames[1])*4.4):0;leftNeedle.style.transform='rotate('+(-142+l*102)+'deg)';rightNeedle.style.transform='rotate('+(-142+r*102)+'deg)';setDuck(speechNow());drawCharacter();raf=requestAnimationFrame(animate)};
  const play=async()=>{if(!music&&!character){status.textContent='Add music or a presenter in the desktop app first.';return}const playing=(music&&!music.paused&&!music.ended)||(character&&!character.paused&&!character.ended);if(playing){music?.pause();character?.pause();setDuck(false);status.textContent='PAUSED â€” TAP COIN TO RESUME';return}try{coin.classList.add('is-accepting');setTimeout(()=>coin.classList.remove('is-accepting'),680);await prepare();if(started&&music?.ended)music.currentTime=0;if(started&&character?.ended)character.currentTime=0;const attempts=[];if(music)attempts.push(music.play());if(character)attempts.push(character.play());await Promise.allSettled(attempts);started=true;machine.classList.add('is-awake');status.textContent='NOW PLAYING â€” '+shareTitle;setDuck(speechNow(),true);if(!raf)raf=requestAnimationFrame(animate)}catch(error){status.textContent='Coin accepted â€” tap again to allow playback.';if(music)music.controls=true;if(character)character.controls=true}};
  const sync=()=>{updateTitleLight();setDuck(speechNow(),true);if(!raf)raf=requestAnimationFrame(animate)};coin.addEventListener('click',play);music?.addEventListener('play',sync);music?.addEventListener('pause',sync);music?.addEventListener('timeupdate',updateTitleLight);music?.addEventListener('seeking',updateTitleLight);music?.addEventListener('seeked',sync);music?.addEventListener('ended',sync);character?.addEventListener('play',sync);character?.addEventListener('pause',sync);character?.addEventListener('seeking',()=>setDuck(false,true));character?.addEventListener('seeked',sync);character?.addEventListener('ended',()=>{setDuck(false);sync()});document.querySelectorAll('.action').forEach(action=>action.addEventListener('click',event=>{if(action.dataset.enabled!=='true')event.preventDefault()}));document.getElementById('share').addEventListener('click',async()=>{const url=shareUrl||location.href;try{if(navigator.share)await navigator.share({title:shareTitle,text:'Open '+shareTitle+' Jukebox',url});else await navigator.clipboard.writeText(url)}catch{}});updateTitleLight();addEventListener('pagehide',()=>{cancelAnimationFrame(raf);context?.close?.()},{once:true});})();
  </script></body></html>`;
}

function esc(value) {
  return String(value || "")
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#39;");
}

function safeDestination(value) {
  const text = String(value || "").trim();
  return /^(?:https:\/\/|mailto:|tel:)/i.test(text) ? text : "";
}

