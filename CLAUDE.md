# Deep Cuts Agent Instructions

Always read `PLATFORM_ARCHITECTURE_DIRECTIVE.md` before every other project instruction. Perform its edition impact assessment before changing anything. If isolation or backward compatibility is uncertain, preserve the completed editions and stop the change unless the owner explicitly authorises the impact.

## Unattended batch constitution

- A future CSV batch is started with **Run the Deep Cuts batch.**
- Every accepted artist must pass a fail-closed 98% identity and direct-destination evidence gate.
- Search-result URLs, placeholders and inferred identities are not publishable evidence.
- One rejected or technically failed artist must never stop another artist.
- Checkpoint every artist, retry temporary failures, resume safely and record every rejection.
- Reuse the shared engine and deployment. Never duplicate the app per artist.
- Never alter Aggits, commit secrets, damage completed editions or ask routine per-artist questions.
- Every production change must reduce manual work and update the batch guide, tests and reports where relevant.

## Commercial Instinct Banjo handoff

Keep `banjo-strategy-brief/1.0` isolated inside `/sell/`. Export only the reviewed speaking script and minimal seller/target provenance. Never include the complete private report, enable voice cloning without explicit consent, or alter Music, Cars, Clubs or Schools for this workflow.

Read `DEEP_CUTS_PRODUCTION_MANUAL.md` completely before acting. Treat it as the project constitution.

Every public product and edition screen must end with the immutable two-line Core Engine footer `Deep Cuts` and `Copyright Clearlight Creative`. No edition configuration, product renderer, quiz, result, error or legal page may override or omit it.

Preserve one reusable discovery engine. Put all band-specific information in edition configuration. Never create another repository or duplicate the application for a band. Protect both approved Aggits files and their integrity hashes.

Treat `Deep Cuts Cars [Make and Model]` as a separately typed automotive edition in the same permanent engine. Preserve the locked Music layout and destinations. Cars content belongs only in `editionType: car` configuration and verified automotive evidence.

Treat `Deep Cuts Clubs [Club name and location]` as a separately typed club edition. Preserve Music and Cars. Club content belongs only in `editionType: club` configuration and identity-checked direct club, membership, events, participation, venue, history, contact and governing-body evidence. Fail closed on generic or login-only social pages.

Treat a Business Recruitment build as `editionType: business`. Preserve every existing product. Require direct current official job pages, a verified company logo and palette, an owner-selected or verified video, and exactly ten sourced positive questions. Edition-specific owner-supplied Aggits artwork must have a locked hash and must never replace the standard Aggits masters.

Treat `JookBox [Band]` as the separately typed `jookbox/3` band model. Preserve every existing product. Never configure Aggits, a quiz or a wheel. The final owner-approved product name is **Mahogany Jukebox Master**, and Savage Garden `dc_e65763b78b` is its approved live reference after the final mobile coin-motion and four-key proportion corrections. Capture the artist-controlled source as dated research evidence, independently verify every published destination and verify the most-viewed embeddable official YouTube video. Every new, migrated or regenerated Band edition must use the permanent `mahogany-jookbox-master/1` appearance and `mahogany-four-key/1`; reject ATLAS, neon and every other cabinet or key format. This is the SHA-256-locked mahogany/brass cabinet with an automatically fitted band marquee, upper right-to-left amber ticker, real coin bay, privacy-enhanced screen, four circular icon-only keys, brass `SHARE` plate and lower copyright hardware. Exactly four verified destinations render; visible key labels are suppressed and Band key lighting is disabled. Fit the band name after fonts load and on resize, using a balanced multiline fallback rather than truncation. The four states are `sleeping`, `acceptingCoin`, `poweringUp` and `awake`; every Band, Bar, Aggits and Studio coin uses the shared real coin recording engine with pre-decoded Web Audio and HTML-audio fallback inside the direct gesture. Synthetic replacement sounds and silent failure handlers are forbidden. Protect the coin recording's mobile media focus before requesting YouTube playback, while retaining the manual control for browser-policy fallback. The awake flag is edition-specific session state. Runtime scraping is forbidden, and only IDs in the edition's curated `displaySelectionIds` may render. Completed alternative editions are frozen solely for route compatibility and cannot be regenerated or used as templates. The ten-edition trial is complete; all future Band work uses the Mahogany Jukebox Master.

For new and trial-migrated Band JookBox delivery only, use `aggits-character-poster-perspective/2`, the owner-approved SHA-256-locked 1254 × 1254 character QR poster. This never adds Aggits to the live Band page. Fit the band title and permanent opaque HTTPS QR into the photographed quadrilateral cabinet opening, then fail closed unless actual full-size, 627-pixel and 360-pixel decoder scan-back all succeed.

Treat Studio **Bar Edition** as the separately typed static `bar-jukebox/1` venue model. It reuses the locked ATLAS cabinet and shared coin/neon/CRT/ticker animation engine without changing Band Edition. Accept only administrator-supplied venue name, fallback ticker, About Us copy, local MP4 and exactly five labelled HTTPS destinations; About Us is always the sixth key, and the support strip is the sole working Share control, visibly labelled exactly `SHARE`. During private publication only, read the configured official Gigs page and persist supported structured upcoming-event facts as the static ticker; never infer missing events and never scrape at public runtime. It has no Aggits, YouTube, quiz or wheel. Its six keys illuminate individually in reading order with the lamp clipped inside each parchment face. Request local MP4 playback only after the real coin recording completes and retain native controls for autoplay fallback. Never encode the private ephemeral `127.0.0.1` Studio preview in a Bar Edition QR; show publication pending and generate the permanent scan-tested QR only from the deployed public HTTPS URL.

Render Bar Edition only with the SHA-256-locked heritage mahogany/brass cabinet asset. Preserve the backward-compatible stored appearance identifier, but present the venue name in its arch, its configured ticker in one large bright yellow line, the six existing keys as parchment/brass hardware, its sole venue-share strip and `Copyright Clearlight Creative 2026.` on a bottom brass plate. Do not apply this Bar-only surface to Band JookBox.

Venue Library may cross that boundary only through its protected publisher. The owner sees one `Published` / `Unpublished` switch; the existing Windows-encrypted credential and all mandatory Cloudflare, R2, email, live-page, MP4 and permanent-QR validations remain behind it. Never store a plaintext credential or mark a venue published early. Unpublish only changes remote active status and must preserve the opaque edition ID, permanent URL, QR payload, assets and audit history; republish runs the full pipeline against current local content while reusing that same identity. The one-time active-library reset archives and hides existing records without changing public state, and `Add Venue` restores them one at a time. Reject public Bar MP4 assets over 24 MiB and roll back any unverified update.

Treat Aggits Jukebox protected publication as separate from Venue Library and both established JookBox products. Use the existing Windows-encrypted Studio device identity but dedicated Aggits Jukebox API routes, D1 tables and R2 prefix. The one-click `PUBLISH + EMAIL QR` action saves the edition, assigns a stable opaque `/e/dc_*` URL, uploads the versioned MP4, generates a scan-tested permanent QR using the locked 1254 x 1254 owner master and automatically requests the completion email. Fit the edition title in the plaque and centre the QR inside the measured bright opening with balanced margins and no overlap onto the cabinet frame. Do not report success until the completion email naming the edition contains a clickable and plain-copy URL plus the QR attachment, delivery is confirmed, and the live page, MP4 and QR all verify. The public standalone cabinet presentation is `aggits-jukebox-oval-master/4`: preserve its fixed AGGITS marquee, vertical real-metal gold coin, exact established coin recording, and one row of four icon-only black/brass oval keys using the 111-icon clean oval-native vector set, including Bandcamp. Retain accessible names without drawing key labels or secondary exterior lines. Keys never illuminate; one key latches inward at a time, plays the local public-domain mechanical clunk, releases the prior key and delays destination navigation by exactly 500 milliseconds. YouTube projects must pass the embedded-player availability check during Create before they can be prepared for publication.

Keep Aggits Jukebox bulk import local, authenticated and draft-only. Use the central importer and explicit source mappings; never infer unknown icons/actions, evaluate spreadsheet formulas, overwrite published editions, or bypass per-edition publication. Preserve stable identities, source checksum, audit results, snapshots and recoverable rollback. Full operator details live in `docs/AGGITS_JUKEBOX_BULK_IMPORT.md`.

Route every JookBox anchor through the shared origin-aware external-link policy. External HTTPS destinations open in a new tab with `target="_blank"` and `rel="noopener noreferrer"`; same-origin and relative links must remain unchanged.

For `mahogany-four-key/1`, require exactly four verified external `displaySelectionIds` and always render four uniform circular icon-only keys. Do not render key labels, gap-fill utilities or Band key lighting. Accessibility names and analytics metadata remain mandatory.

Lock `mahogany-jookbox-master/1` as the mandatory Band factory presentation from 2026-08-04 onward, not an optional default. Its brass plate is the working `SHARE` action and its lower hardware displays `Copyright Clearlight Creative 2026.` while the immutable global footer remains unchanged. Use exactly four verified icon-only destinations, no Band button light effects, and never fabricate destinations. Block ATLAS at create, import, regeneration and validation boundaries. Preserve completed non-trial ATLAS routes as frozen products only.

Treat `Laneway [Band or Artist]` as a separately typed Laneway edition. Preserve Music, Cars, Clubs and Schools. Laneway alone uses the approved reverse-white Laneway Music logo, charcoal/black styling, no Aggits, and exactly five positive sourced questions in its isolated quiz engine.

Treat Record Company Edition as a separately typed parent/child ecosystem. Preserve all other products. One official URL starts the whole accessible roster; do not ask for per-artist approval. Enforce 98% identity and question evidence, isolate failures, verify every QR twice, reconcile reporting and complete email delivery before reporting success.

Preserve Laneway's locked discovery order: `Listen / Watch / Discover / Buy`, official-label-selected YouTube feature, verified destinations, five-question challenge, then `Home / Share / Recommended`. Never replace the official Laneway Music navigation URLs with guesses.

Treat `indie_label/1` as the final locked independent-label wheel model. Its reference is restored Laneway company edition `dc_b9e7b66620`, including the conditional verified winner-video panel and its pale-teal decorative system with red reserved for the wheel pointer; never reintroduce the rejected catalogue-discovery overhaul. Future label editions use isolated `indie_wheel` configuration and may change only their own verified brand, colours, roster, destination platform, links, copy, quiz, contact and source-credit metadata. Never modify the global footer, the Laneway reference or Cool Death Records to create another label.

Continually reduce manual work, GitHub interaction, PowerShell interaction, copy/paste and prompts. Automate research capture, configuration, link validation, artwork, scan-back testing, commits, deployment and delivery wherever credentials permit. Never ask the non-technical owner to perform repetitive engineering work that the system can safely perform.

Use only verified official destinations. Omit unavailable buttons completely. When an official YouTube presence exists, verify and feature the most-viewed official music video; never invent one. The tipping feature is retired. Analytics must be best-effort and must never block a link.

Update the manual, README, roadmap, skill and tests whenever the workflow changes. Every future edition must be faster than the previous edition. At the end of every task ask what can be automated next, then implement it when practical.

Mahogany Jukebox 1.4.12 adds an explicit optional skin exception to the fixed-cabinet rule. New custom skins are verified 941 × 1672 PNG, JPEG or WebP files of at most 12 MiB, aligned to the immutable `MASTER STRUCTURE.png`, and replace only the cabinet background. Do not move, restyle or change the coin, sound, media, ticker, four oval keys, share, analytics, QR, publication or delivery layer. Preserve stored 864 × 1536 projects through the isolated legacy profile; never silently migrate them. Publish custom skins as versioned, SHA-256-verified assets and include them in rollback and live verification.
