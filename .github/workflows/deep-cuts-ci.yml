name: Deep Cuts validation

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: node scripts/validate-platform.mjs
      - run: node scripts/validate-questions.mjs
      - run: node scripts/test-engine.mjs
      - run: node scripts/validate-questions.mjs business-preview
      - run: node scripts/test-engine.mjs business-preview
      - run: node scripts/validate-questions.mjs access-workwear-safety
      - run: node scripts/test-engine.mjs access-workwear-safety
      - run: node scripts/test-build-tracking.mjs
      - run: node --check js/app.js
      - run: node --check js/engine.js
      - run: node --check scripts/build-tracker.mjs
      - run: node --check scripts/track-build.mjs
      - run: node --check scripts/publish-edition.mjs
      - run: node --check scripts/prepare-delivery.mjs
      - run: python scripts/generate-social-assets.py

